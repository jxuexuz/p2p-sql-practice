# 采购到付款业务流程模拟（p2p-sql-practice）

> 面向 ERP 实施助理学习的个人 MySQL 模拟项目：从采购订单出发，覆盖分批验收入库、应付确认、付款核销、采购退货，并跑出应付账龄、到货及时率和异常检查。

![MySQL](https://img.shields.io/badge/MySQL-8.4-4479A1?logo=mysql&logoColor=white)
![DBeaver](https://img.shields.io/badge/Client-DBeaver-382923)
![SQL](https://img.shields.io/badge/Language-SQL-F29111)
![Views](https://img.shields.io/badge/Includes-Views%20%26%20Acceptance%20Tests-success)
![Practice](https://img.shields.io/badge/Type-Personal%20Practice-lightgrey)
![License](https://img.shields.io/badge/License-MIT-green)

项目地址：<https://github.com/jxuexuz/p2p-sql-practice>

---

## 目录

- [项目简介](#项目简介)
- [范围和关键区别](#范围和关键区别)
- [核心业务规则](#核心业务规则)
- [示例结果](#示例结果)
- [项目结构](#项目结构)
- [环境要求](#环境要求)
- [本地复现](#本地复现)
- [脚本说明](#脚本说明)
- [验收与证据](#验收与证据)
- [学习要点](#学习要点)
- [文档导航](#文档导航)
- [免责声明](#免责声明)
- [License](#license)

---

## 项目简介

面向 **ERP 实施助理**学习的个人 MySQL 模拟项目，覆盖采购到付款（Procure-to-Pay, P2P）主流程：

- 采购订单
- 分批验收入库
- 应付确认
- 付款核销
- 采购退货

数据和企业名称均为虚构，脚本及文档由 AI 协助搭建，供学习者逐步运行、理解和改进，**不代表真实客户实施、U8 操作或生产系统**。

---

## 范围和关键区别

- **5 张核心表 + 3 张辅助表，不是仅有 5 张表**：供应商、采购订单、入库、应付、付款，以及退货、应付冲减、付款核销。
- **一张订单一种商品**，一次入库最多确认一笔应付。支持：
  - 分批入库
  - 部分付款
  - 同一付款核销多笔应付
  - 同一应付由多次付款核销
- **退货减少净收货数量；另外登记冲减才减少应付**。付款记录表示资金支出，核销记录才表示清偿哪笔应付。
- 所有报表都是查询，**不自动下单、付款、发票认证或阻止超量写入**。外键只保证引用存在，不保证所有业务规则成立。
- 人民币、不含税差和运费；不含付款退款、补货、取消订单、订单变更、会计凭证或并发控制。**并非完整 ERP。**

---

## 核心业务规则

1. **订单 ↔ 商品**：一张订单只对应一种商品，便于把学习重点放在"入库—应付—付款—核销"的资金链上；
2. **入库 → 应付**：每笔入库在达到确认条件时生成一笔应付；分批入库分别生成应付；
3. **退货**：只减少"净收货数量"，不直接动应付；必须再登记一笔应付冲减，应付余额才减少；
4. **付款 ≠ 核销**：付款表记录钱付出去了多少；核销表记录这笔钱具体冲抵了哪几笔应付。二者通过核销关系连接；
5. **多对多核销**：一笔付款可以核销多笔应付，一笔应付也可以被多笔付款分次核销；
6. **未核销付款**：付出去但还没指定冲哪笔应付的钱，不能从应付里再扣一次；
7. **单位隔离**：不同商品数量单位不同（支/本/个/台/包），不汇总成"总库存"或"总数量"。

---

## 示例结果

演示基准日为 **2026-09-25**；订单和当前余额报表汇总全部现有记录，账龄和及时率按脚本中明确的截止日期计算。

### 订单余额

| 订单 | 商品 | 订购数量 | 累计入库 | 退货 | 净收货 | 原应付金额 | 冲减 | 已核销 | 未结清 |
| ---: | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 中性笔（支） | 100 | 100 | 10 | 90 | 200 | 20 | 90 | **90** |
| 2 | 笔记本（本） | 50 | 30 | 0 | 30 | 300 | 0 | 100 | **200** |
| 3 | 文件夹（个） | 20 | 20 | 0 | 20 | 100 | 0 | 0 | **100** |
| 4 | 订书机（台） | 10 | 0 | 0 | 0 | 0 | 0 | 0 | **0** |
| 5 | 打印纸（包） | 10 | 10 | 0 | 10 | 100 | 0 | 100 | **0** |

- 未结清应付合计：700 − 20 − 290 = **390 元**；
- 已付款 310 元，其中 290 元已核销，**20 元尚未核销**（付款 5）；
- 不能把 310 元全部从应付中再扣一次——这正是"付款 ≠ 核销"要解决的问题。

### 账龄与到货及时率

- 供应商 1 未结清 90 元，均在 0–30 天；
- 供应商 2 未结清 300 元，其中 31–60 天 200 元、61 天以上 100 元；
- 供应商 3 无交易但保留显示；
- 到期订单 4 笔，按时足量 2 笔，整体及时率 **50%**；
- 两家有订单供应商各 50%，无到期订单的供应商显示 `NULL`（无适用分母）。

---

## 项目结构

```
p2p-sql-practice/
├── docs/                 # 业务规则、分阶段学习、测试预期、上传步骤、解释提纲、验证记录
├── evidence/             # 统一尺寸的复核结果图与原始输出（verification_results.json）
├── sql/
│   ├── 01_schema.sql                 # 建库 + 8 张表（仅一次）
│   ├── 02_seed.sql                   # 演示数据（事务包裹）
│   ├── 03_views.sql                  # 3 个汇总视图（仅一次）
│   ├── 04_order_and_payment_report.sql  # 订单 / 付款 / 核销明细
│   ├── 05_ap_aging.sql               # 应付账龄
│   ├── 06_delivery_timeliness.sql    # 到货及时率
│   ├── 07_exception_checks.sql       # 异常检查视图（首次建视图）
│   ├── 08_acceptance_tests.sql       # 三组只读验收断言
│   └── 09_transaction_experiments.sql # 事务实验（可选，勿改成 COMMIT）
├── tools/                # 辅助脚本
├── .gitignore
└── README.md
```

---

## 环境要求

- **MySQL 8.4**（utf8mb4，UTF-8）
- **DBeaver**（或任意兼容 MySQL 8.4 的客户端）
- **无需安装 Python，也无需 U8 或其他 ERP**
- 使用本地测试库，**勿连接生产环境**

---

## 本地复现

> 01、02、03、07 的初始化部分**只能执行一次**；04、05、06、08 可重复查询。不要一键反复执行整个目录，遇到错误不要选择"忽略继续"。

1. 在 DBeaver 关联本地 MySQL 连接，先运行：

   ```sql
   SELECT VERSION();
   SHOW DATABASES LIKE 'p2p_portfolio_demo';
   ```

   第二条应返回 0 行。如果已有同名库，停止并核实，不要删除；可统一更换所有 SQL 中的库名。
2. `sql/01_schema.sql`：创建新演示库和 8 张表，**仅执行一次**。不要只选建表语句而跳过 `USE`。如报错立即停止。原 `inventory_practice` 和 `inventory_portfolio_demo` 不受影响。
3. `sql/02_seed.sql`：执行 `USE`、`START TRANSACTION` 及全部 `INSERT`；无错误才 `COMMIT`，否则 `ROLLBACK`。核对表行数为 **3、5、5、5、5、1、1、5**。
4. `sql/03_views.sql`：首次创建 3 个汇总视图，仅执行一次。视图不是业务数据表。
5. `sql/04_order_and_payment_report.sql`：查询订单、付款和核销明细，核对上面的结果。
6. `sql/05_ap_aging.sql` 和 `sql/06_delivery_timeliness.sql`：每次都连同 `SET`（截止日期）一起执行，避免截止日期丢失。
7. `sql/07_exception_checks.sql`：首次创建异常视图；正常数据异常 0 行，待办 1 行（付款 5 未核销 20 元）。以后只执行末尾 `SELECT`，不重复 `CREATE VIEW`。
8. `sql/08_acceptance_tests.sql`：三组只读验收应为 **12 + 4 + 5 = 21 行 PASS**。
9. 可选 `sql/09_transaction_experiments.sql`：按块测试实际表中的异常和回滚。**阅读注释后再操作，不要改成 COMMIT。**

---

## 脚本说明

| 脚本 | 类型 | 可重复 | 预期结果 |
| --- | --- | --- | --- |
| `01_schema.sql` | 写入 | 仅一次 | 建出 `p2p_portfolio_demo` 与 8 张表 |
| `02_seed.sql` | 写入（事务） | 仅一次 | 8 张表行数 3/5/5/5/5/1/1/5 |
| `03_views.sql` | 写入（视图） | 仅一次 | 建成 3 个汇总视图 |
| `04_order_and_payment_report.sql` | 只读查询 | 可重复 | 5 行订单余额 + 付款/核销明细 |
| `05_ap_aging.sql` | 只读查询 | 可重复 | 按 0–30 / 31–60 / 61+ 分组 |
| `06_delivery_timeliness.sql` | 只读查询 | 可重复 | 整体及时率 50% |
| `07_exception_checks.sql` | 视图 + 查询 | 视图仅一次 | 异常 0 行，待办 1 行（20 元未核销） |
| `08_acceptance_tests.sql` | 只读查询 | 可重复 | 21 行 PASS |
| `09_transaction_experiments.sql` | 实验 | 手动 | 观察回滚，勿 COMMIT |

---

## 验收与证据

- 2026-09-25，学习者反馈 01–09 已执行且无报错；
- 已有截图核实 8 张表、初始行数和订单余额；
- 独立 MySQL 8.4.11 实例另行复核通过 **32 组断言**（含 08 的 21 行 PASS 检查）；
- 本机版本仍待查询确认；详见 `evidence/` 下验证记录，**勿混淆两种证据来源**。

[查看 7 张统一尺寸的复核结果图及原始输出说明](evidence/)。下图为真实 SQL 输出排版图，不是 DBeaver 操作截图。

---

## 学习要点

- **付款与核销分离**：这是 P2P 里最容易绕晕的点——钱付了不等于应付清了；
- **多对多核销**：用一张核销表把"付款"和"应付"连起来，而不是在付款表里直接写应付 ID；
- **退货两层处理**：库存减靠入库负数/退货单，应付减必须再走冲减单；
- **账龄截止日**：账龄是"到某一天"的账龄，脚本里用 `SET` 固定，不能裸跑；
- **分母为 0**：没有到期订单的供应商，及时率应该是 `NULL` 而不是 0 或 100%；
- **视图 vs 表**：视图是查询封装，不存业务数据；重复 `CREATE VIEW` 会报错；
- **验收断言**：用一组 `SELECT ... AS expect_pass` 把"应是 21 行 PASS"固定下来，改脚本后能立刻发现回归。

---

## 文档导航

- [业务规则和表结构](docs/)
- [分阶段学习和截图清单](docs/)
- [测试预期与验收](docs/)
- [GitHub 网页上传步骤](docs/)
- [解释提纲与简历用语](docs/)
- [验证记录](docs/)

> 链接指向 `docs/` 目录下对应文档，实际文件名以仓库为准。

---

## 免责声明

- 本项目是**个人学习练习**，所有公司、供应商、订单、金额均为虚构；
- 不代表任何真实客户实施、用友 U8 操作或生产系统；
- 脚本及文档由 AI 协助搭建，我没有把 AI 协助包装成独立开发经历；
- 范围刻意做了裁剪：不含税差、运费、退款、补货、订单变更、会计凭证与并发控制，**不是完整 ERP**；
- 请勿在生产数据库上运行本项目脚本。

---

## License

MIT License
