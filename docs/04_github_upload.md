# GitHub网页上传

目标仓库所有者为`jxuexuz`，仓库名为`p2p-sql-practice`。项目包不会自动创建或上传仓库，不需要GitHub桌面软件。

## 先检查再上传

1. 本地解压，确认目录包含`README.md`、`.gitignore`、`sql`、`docs`、`evidence`、`tools`。不要直接上传ZIP文件。evidence包含结果图和原始输出，tools仅用于可选的重新制图。
2. 先运行并核对项目。没有复跑也可作为“学习中”发布，但不能改成“本人已完成验证”。
3. 用浏览器登录自己的GitHub，确认账户确实是jxuexuz，不要发送密码或验证码给他人。
4. 如果该账户下已有同名仓库，先查看内容，不要删除、覆盖或重复建库；把页面截图发给协助者确认下一步。
5. 若不存在，新建仓库，名称填`p2p-sql-practice`。描述可用：“采购到付款SQL模拟实践：订单履约、付款核销、应付账龄与到货及时率。”
6. 用于求职展示可选择Public（任何人可看）。如果暂不准备公开则选Private。初始化README、.gitignore和许可证暂不勾选，本包已经包含前两者，许可证由你自行决定。
7. 创建后点“uploading an existing file/上传已有文件”。把解压后项目文件夹**里面的内容**拖入，而不是把外层目录或ZIP拖进去。
8. 上传列表应是根目录README、.gitignore，以及sql/01_schema.sql等路径；避免`p2p-sql-practice/p2p-sql-practice/...`多套一层。
9. 提交说明可填“添加采购到付款SQL模拟脚本与学习文档”，点Commit changes。
10. 进入仓库首页检查README显示、sql和docs目录、文档链接和SQL中文内容。目标地址应为`https://github.com/jxuexuz/p2p-sql-practice`。

## 更新状态

发布成功后，将README中的“计划使用的地址”改为实际项目链接，记录实际提交日期。完成个人复跑后，更新验证记录，并只上传裁剪检查过的结果截图。

不要上传DBeaver工作区、连接配置、数据库数据文件、完整备份、个人简历、身份证件或验证码截图。网页上传也要手动审查，不能依赖.gitignore自动拦截。

如果页面与你看到的说明不同，停在当前页面截图求助，不必猜按钮。截图应避开二维码、密码、令牌和验证码。
